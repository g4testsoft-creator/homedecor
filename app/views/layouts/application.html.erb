<!DOCTYPE html>
<html>
  <head>
    <title>Homedecor</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <style>
      .main-nav {
        background: white;
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        padding: 0;
        margin-bottom: 0;
      }

      .nav-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 16px 20px;
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      .nav-logo {
        font-size: 1.5rem;
        font-weight: 600;
        color: #2c3e50;
        text-decoration: none;
      }

      .nav-logo:hover {
        color: #34495e;
      }

      .nav-links {
        display: flex;
        align-items: center;
        gap: 16px;
      }

      .nav-user {
        color: #666;
        font-size: 0.9rem;
      }

      .nav-link {
        padding: 8px 16px;
        border-radius: 6px;
        text-decoration: none;
        color: #333;
        font-size: 0.95rem;
        transition: background 0.2s ease;
      }

      .nav-link:hover {
        background: #f5f5f5;
      }

      .nav-link-primary {
        background: #2c3e50;
        color: white;
      }

      .nav-link-primary:hover {
        background: #34495e;
        color: white;
      }

      .notice, .alert {
        padding: 12px 20px;
        margin: 0;
        text-align: center;
        font-size: 0.95rem;
      }

      .notice {
        background: #d4edda;
        color: #155724;
        border-bottom: 1px solid #c3e6cb;
      }

      .alert {
        background: #f8d7da;
        color: #721c24;
        border-bottom: 1px solid #f5c6cb;
      }

      body {
        margin: 0;
        padding: 0;
      }

      /* Auth pages (Sign in / Sign up) */
      .auth-page {
        min-height: calc(100vh - 120px);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 40px 20px;
        background: linear-gradient(145deg, #f6f8fb 0%, #eef1f5 100%);
      }

      .auth-card {
        width: 100%;
        max-width: 420px;
        background: white;
        border-radius: 16px;
        box-shadow: 0 10px 40px rgba(0, 0, 0, 0.08), 0 2px 8px rgba(0, 0, 0, 0.04);
        padding: 40px 36px;
      }

      .auth-title {
        margin: 0 0 6px 0;
        font-size: 1.75rem;
        font-weight: 600;
        color: #2c3e50;
        letter-spacing: -0.02em;
      }

      .auth-subtitle {
        margin: 0 0 28px 0;
        font-size: 0.95rem;
        color: #64748b;
      }

      .auth-form {
        display: flex;
        flex-direction: column;
        gap: 20px;
      }

      .auth-field {
        display: flex;
        flex-direction: column;
        gap: 6px;
      }

      .auth-field-checkbox {
        flex-direction: row;
        align-items: center;
        gap: 10px;
      }

      .auth-label {
        font-size: 0.9rem;
        font-weight: 500;
        color: #374151;
      }

      .auth-hint {
        font-size: 0.8rem;
        color: #64748b;
        font-weight: 400;
      }

      .auth-input {
        width: 100%;
        padding: 12px 14px;
        font-size: 1rem;
        line-height: 1.4;
        color: #1f2937;
        background: #fafafa;
        border: 1px solid #e5e7eb;
        border-radius: 10px;
        box-sizing: border-box;
        transition: border-color 0.2s ease, background 0.2s ease;
      }

      .auth-input::placeholder {
        color: #9ca3af;
      }

      .auth-input:hover {
        border-color: #d1d5db;
      }

      .auth-input:focus {
        outline: none;
        border-color: #2c3e50;
        background: white;
        box-shadow: 0 0 0 3px rgba(44, 62, 80, 0.12);
      }

      .auth-checkbox {
        width: 18px;
        height: 18px;
        accent-color: #2c3e50;
      }

      .auth-checkbox-label {
        font-size: 0.9rem;
        color: #4b5563;
        font-weight: 400;
        cursor: pointer;
      }

      .auth-actions {
        margin-top: 8px;
      }

      .auth-submit {
        width: 100%;
        padding: 14px 20px;
        font-size: 1rem;
        font-weight: 600;
        color: white;
        background: #2c3e50;
        border: none;
        border-radius: 10px;
        cursor: pointer;
        transition: background 0.2s ease, transform 0.05s ease;
      }

      .auth-submit:hover {
        background: #34495e;
      }

      .auth-submit:active {
        transform: scale(0.99);
      }

      .auth-links-wrap {
        margin-top: 24px;
        padding-top: 24px;
        border-top: 1px solid #eee;
      }

      .auth-links {
        text-align: center;
        font-size: 0.9rem;
        color: #64748b;
      }

      .auth-link {
        color: #2c3e50;
        text-decoration: none;
        font-weight: 500;
      }

      .auth-link:hover {
        text-decoration: underline;
      }

      .auth-links-sep {
        margin: 0 8px;
        color: #cbd5e1;
      }

      .auth-link-button {
        background: none;
        border: none;
        padding: 0;
        font: inherit;
        cursor: pointer;
      }

      .auth-errors {
        padding: 14px 16px;
        margin-bottom: 8px;
        background: #fef2f2;
        border: 1px solid #fecaca;
        border-radius: 10px;
        font-size: 0.9rem;
      }

      .auth-errors-title {
        margin: 0 0 8px 0;
        font-weight: 600;
        color: #991b1b;
      }

      .auth-errors-list {
        margin: 0;
        padding-left: 20px;
        color: #b91c1c;
      }

      .auth-card-danger {
        margin-top: 24px;
        border: 1px solid #fecaca;
        background: #fffbeb;
      }

      .auth-submit-danger {
        background: #dc2626;
      }

      .auth-submit-danger:hover {
        background: #b91c1c;
      }
    </style>
  </head>

  <body>
    <nav class="main-nav">
      <div class="nav-container">
        <%= link_to "Home Decor", root_path, class: "nav-logo" %>
        <div class="nav-links">
          <% if user_signed_in? %>
            <span class="nav-user">
              Welcome, <%= current_user.full_name.presence || current_user.email %>
            </span>
            <% if current_user.admin? %>
              <%= link_to "Admin", admin_decor_items_path, class: "nav-link" %>
            <% end %>
            <%= link_to "Sign out", destroy_user_session_path, data: { turbo_method: :delete }, class: "nav-link" %>
          <% else %>
            <%= link_to "Sign in", new_user_session_path, class: "nav-link" %>
            <%= link_to "Sign up", new_user_registration_path, class: "nav-link nav-link-primary" %>
          <% end %>
        </div>
      </div>
    </nav>

    <% if notice %>
      <p class="notice"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert"><%= alert %></p>
    <% end %>

    <%= yield %>
  </body>
</html>
